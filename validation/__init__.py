"""
Validation Layer — enforces schema, safety, and deterministic checks
on all agent inputs, outputs, tool calls, and MCP responses.

Import:
    from validation.layer import ValidationLayer, ValidationResult, SecurityBlockedError
    from validation.rules import RuleEngine
    from validation.schemas import ValidationDecision
"""
