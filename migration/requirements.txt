# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt
# Python dependencies for the Legacy → Salesforce migration pipeline.
#
# Install: pip install -r requirements.txt
#
# For a reproducible environment use the pinned lockfile:
#   pip install -r requirements-lock.txt
#
# Author  : Migration Platform Team
# Modified: 2026-03-16
# ─────────────────────────────────────────────────────────────────────────────

# ─── Salesforce API ───────────────────────────────────────────────────────────
simple-salesforce>=1.12.5,<2.0.0     # SOAP + REST + Bulk API client

# ─── Database / ORM ───────────────────────────────────────────────────────────
SQLAlchemy>=2.0.0,<3.0.0             # Database abstraction layer
pyodbc>=4.0.39                       # ODBC driver (MSSQL/SQL Server)
pymysql>=1.1.0                       # MySQL/MariaDB driver (optional)
psycopg2-binary>=2.9.7               # PostgreSQL driver (optional)

# ─── Data Processing ─────────────────────────────────────────────────────────
pandas>=2.1.0,<3.0.0                 # DataFrame operations
pyarrow>=14.0.0,<16.0.0             # Parquet read/write, columnar format
numpy>=1.26.0,<2.0.0                # Numerical operations
fastparquet>=2023.8.0                # Alternative Parquet engine (optional)

# ─── Configuration ────────────────────────────────────────────────────────────
PyYAML>=6.0.1                        # YAML config file parsing
python-dotenv>=1.0.0                 # .env file support for local dev
pydantic>=2.5.0,<3.0.0              # Config validation and settings

# ─── HTTP / API ───────────────────────────────────────────────────────────────
requests>=2.31.0,<3.0.0             # HTTP client
urllib3>=2.0.7,<3.0.0               # HTTP lower-level (requests dep)
httpx>=0.26.0                       # Async HTTP client (optional)

# ─── Data Validation ─────────────────────────────────────────────────────────
great-expectations>=0.18.0,<1.0.0   # Advanced data quality framework (optional)
cerberus>=1.3.4                     # Lightweight schema validation

# ─── Orchestration ────────────────────────────────────────────────────────────
apache-airflow>=2.8.0,<3.0.0        # Primary pipeline orchestrator
# OR Prefect (alternative):
# prefect>=2.14.0,<3.0.0

# ─── Reporting / Visualisation ───────────────────────────────────────────────
matplotlib>=3.8.0,<4.0.0            # Chart generation for HTML reports
jinja2>=3.1.0                       # HTML template rendering
openpyxl>=3.1.0                     # Excel report export

# ─── Logging / Monitoring ─────────────────────────────────────────────────────
structlog>=24.1.0                   # Structured logging
sentry-sdk>=1.40.0                  # Error tracking (optional)

# ─── Security / Crypto ────────────────────────────────────────────────────────
cryptography>=42.0.0                # Encryption utilities
keyring>=24.3.0                     # Secure credential storage

# ─── CLI ──────────────────────────────────────────────────────────────────────
click>=8.1.7                        # CLI framework
rich>=13.7.0                        # Terminal rich output
tqdm>=4.66.0                        # Progress bars

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0                       # Test runner
pytest-cov>=4.1.0                   # Coverage plugin
pytest-mock>=3.12.0                 # Mocking
responses>=0.24.0                   # HTTP mock for requests
factory-boy>=3.3.0                  # Test data factories
freezegun>=1.4.0                    # DateTime mocking

# ─── Code Quality ─────────────────────────────────────────────────────────────
black>=23.12.0                      # Code formatter
isort>=5.13.0                       # Import sorter
flake8>=7.0.0                       # Linter
mypy>=1.8.0                         # Static type checking
pre-commit>=3.6.0                   # Pre-commit hooks

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dateutil>=2.8.2              # Date parsing utilities
pytz>=2024.1                        # Timezone handling
tenacity>=8.2.3                     # Retry library
more-itertools>=10.2.0              # Extra itertools utilities
tabulate>=0.9.0                     # ASCII table rendering
